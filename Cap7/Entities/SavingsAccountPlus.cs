namespace CursoDeCsharp.Cap7.Entities
{
    /* Tente em casa retirar a parte da frente da chamada de nossa classe, onde esta tentando herdar a classe Savings
    Account, e veja que não sera possível, já que selamos a mesma. */
    class SavingsAccountPlus /* : SavingsAccount */
    {
    }
}